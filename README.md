# GaezyAvito

Проект для парсинга объявлений с Avito и предоставления доступа к ним через Telegram бота.

## 🏗️ Архитектура проекта

Проект состоит из двух основных сервисов:

1. **parser_avito** - REST API сервис для парсинга объявлений с Avito
2. **telegram_bot** - Telegram бот для взаимодействия с пользователями

## 🛠️ Используемые технологии

### Парсер Avito (`parser_avito`)

- **Python 3.11** - основной язык разработки
- **FastAPI** - веб-фреймворк для создания REST API
- **Uvicorn** - ASGI сервер для запуска FastAPI приложения
- **Playwright** - автоматизация браузера для парсинга
- **Playwright Stealth** - обход обнаружения автоматизации
- **BeautifulSoup4** - парсинг HTML
- **curl_cffi** - HTTP клиент с обходом Cloudflare
- **Pydantic** - валидация данных
- **Loguru** - логирование
- **python-dotenv** - загрузка переменных окружения

### Telegram бот (`telegram_bot`)

- **Python 3.11** - основной язык разработки
- **Aiogram 3.22** - асинхронный фреймворк для Telegram ботов
- **python-telegram-bot** - дополнительная библиотека для работы с Telegram API
- **SQLAlchemy 2.0** - ORM для работы с базой данных
- **asyncpg** - асинхронный драйвер PostgreSQL
- **PostgreSQL** - реляционная база данных
- **Pydantic** - валидация данных
- **aiohttp** - асинхронный HTTP клиент
- **python-dotenv** - загрузка переменных окружения

### Инфраструктура

- **Docker** - контейнеризация приложений
- **Docker Compose** - оркестрация контейнеров

## 📋 Требования

- Docker и Docker Compose
- Python 3.11+ (для локальной разработки)
- PostgreSQL 12+ (для локальной разработки)

## 🚀 Развертывание

### Развертывание в Docker (Production)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd GaezyAvito
   ```

2. **Настройте переменные окружения:**
   
   Создайте файлы `.env` на основе шаблонов:
   
   - Скопируйте `parser_avito/.env.template` в `parser_avito/.env` и заполните переменные
   - Скопируйте `telegram_bot/.env.template` в `telegram_bot/.env` и заполните переменные

3. **Запустите сервисы:**
   ```bash
   docker-compose up -d
   ```

4. **Проверьте статус контейнеров:**
   ```bash
   docker-compose ps
   ```

5. **Просмотр логов:**
   ```bash
   # Все сервисы
   docker-compose logs -f
   
   # Конкретный сервис
   docker-compose logs -f parser_avito
   docker-compose logs -f telegram_bot
   ```

6. **Остановка сервисов:**
   ```bash
   docker-compose down
   ```

### Развертывание в режиме разработки (Development)

#### Парсер Avito

1. **Перейдите в директорию сервиса:**
   ```bash
   cd parser_avito
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python3.11 -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Установите браузеры для Playwright:**
   ```bash
   playwright install --with-deps
   ```

5. **Настройте переменные окружения:**
   
   Создайте файл `.env` в директории `parser_avito/` на основе `parser_avito/.env.template`

6. **Запустите сервис:**
   ```bash
   uvicorn api:app --host 0.0.0.0 --port 8000 --reload
   ```

   Сервис будет доступен по адресу: `http://localhost:8000`

#### Telegram бот

1. **Убедитесь, что PostgreSQL запущен и создана база данных:**
   ```bash
   # Пример для локальной установки PostgreSQL
   createdb gaezy_avito
   ```

2. **Перейдите в директорию сервиса:**
   ```bash
   cd telegram_bot
   ```

3. **Создайте виртуальное окружение:**
   ```bash
   python3.11 -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Настройте переменные окружения:**
   
   Создайте файл `.env` в директории `telegram_bot/` на основе `telegram_bot/.env.template`
   
   Убедитесь, что `DATABASE_URL` указывает на вашу локальную базу данных:
   ```
   DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/gaezy_avito
   ```

6. **Запустите бота:**
   ```bash
   python run.py
   ```

## 📝 Переменные окружения

### Парсер Avito (`parser_avito/.env`)

См. файл `parser_avito/.env.template` для списка всех переменных.

**Обязательные:**
- `API_TOKEN` - токен для аутентификации в API парсера

**Опциональные:**
- `PROXY_STRING` - строка прокси в формате `http://user:pass@host:port`
- `PROXY_CHANGE_URLS` - список URL для смены IP через `|` (например: `url1|url2|url3`)
- `GEO` - географический регион поиска

### Telegram бот (`telegram_bot/.env`)

См. файл `telegram_bot/.env.template` для списка всех переменных.

**Обязательные:**
- `BOT_TOKEN` - токен Telegram бота от @BotFather
- `DATABASE_URL` - строка подключения к PostgreSQL (формат: `postgresql+asyncpg://user:password@host:port/dbname`)
- `PARSER_API_URL` - URL API парсера (например: `http://localhost:8000`)
- `PARSER_API_TOKEN` - токен для доступа к API парсера (должен совпадать с `API_TOKEN` в парсере)
- `API_TOKEN` - внутренний токен бота
- `YOOKASSA_TOKEN` - токен YooKassa для обработки платежей

## 🔧 Конфигурация Docker

Проект использует Docker Compose для оркестрации сервисов. Конфигурация находится в файле `docker-compose.yml`.

Оба сервиса используют:
- **network_mode: host** - для доступа к локальной сети
- **restart: unless-stopped** - автоматический перезапуск при падении

## 📚 API документация

После запуска парсера, документация API доступна по адресу:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## 🗄️ База данных

Telegram бот использует PostgreSQL для хранения:
- Пользователей и их подписок
- Отслеживаемых объявлений
- Платежей и промокодов

Схема базы данных автоматически создается при первом запуске бота через SQLAlchemy.

## 📂 Структура проекта

```
GaezyAvito/
├── parser_avito/          # Сервис парсинга Avito
│   ├── api.py            # FastAPI приложение
│   ├── Dockerfile        # Docker образ парсера
│   ├── requirements.txt  # Зависимости Python
│   ├── .env.template     # Шаблон переменных окружения
│   └── src/              # Исходный код парсера
│       ├── config.py     # Конфигурация
│       ├── parser_cls.py # Класс парсера
│       └── ...
├── telegram_bot/          # Telegram бот
│   ├── app/              # Исходный код приложения
│   │   ├── bot/          # Обработчики бота
│   │   ├── db/           # Модели и репозитории БД
│   │   ├── services/     # Бизнес-логика
│   │   └── ...
│   ├── run.py           # Точка входа
│   ├── Dockerfile       # Docker образ бота
│   ├── requirements.txt # Зависимости Python
│   └── .env.template    # Шаблон переменных окружения
├── docker-compose.yml    # Конфигурация Docker Compose
└── README.md            # Документация
```

## 🔍 Логирование

Логи парсера сохраняются в:
- `parser_avito/logs/api.log` - логи API
- `parser_avito/logs/app.log` - логи приложения

Логи бота выводятся в консоль и могут быть настроены через `app/utils/logging_config.py`.

## ⚠️ Важные замечания

1. **Playwright требует установки системных зависимостей** - при использовании Docker они устанавливаются автоматически через `playwright install --with-deps`
2. **Для работы бота необходим доступ к PostgreSQL** - убедитесь, что база данных доступна
3. **Токены API должны совпадать** - `PARSER_API_TOKEN` в боте должен совпадать с `API_TOKEN` в парсере
4. **Сетевая конфигурация** - сервисы используют `network_mode: host`, что требует соответствующих прав Docker

## 🐛 Решение проблем

### Парсер не запускается

- Проверьте, что установлены все зависимости: `pip install -r requirements.txt`
- Убедитесь, что Playwright браузеры установлены: `playwright install --with-deps`
- Проверьте логи: `docker-compose logs parser_avito`

### Бот не подключается к базе данных

- Проверьте правильность `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен и база данных существует
- Проверьте доступность базы данных с вашего хоста

### Ошибки с прокси

- Проверьте формат `PROXY_STRING`
- Убедитесь, что прокси доступен и работает

## 📄 Лицензия

См. файл `LICENSE` для информации о лицензии.




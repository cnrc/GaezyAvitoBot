"""
Поиск объявлений на Avito
"""
from aiogram import Router, types
from app.db import user_has_active_subscription

router = Router()

# Функция отслеживания по фильтрам временно отключена
# так как отсутствует модуль parse_avito_client
# Используйте отслеживание конкретных объявлений по ссылкам

# @router.message(lambda message: "|" in message.text and "avito.ru" not in message.text.lower() if message.text else False)
# async def handle_tracking_by_filters(message: types.Message):
#     """Обработчик добавления отслеживания по фильтрам - ОТКЛЮЧЕН"""
#     await message.answer(
#         "🔧 Отслеживание по фильтрам временно недоступно.\n\n"
#         "Используйте отслеживание конкретных объявлений по ссылкам через кнопку '➕ Добавить отслеживание'.",
#         parse_mode="HTML"
#     )


